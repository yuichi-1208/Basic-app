<h3>以下の内容で、送信する。</h3>
<%= form_with(model: @blog, local: true) do |form| %>
<%# hidden_fieldにしないとparameterが送れない。hidden_fieldは値を保持したまま、ビューで内容を隠してくれる。絶対必要！ %>
  <%= form.hidden_field :title %>
  <%= form.hidden_field :content %>
  <p>タイトル:<%= @blog.title %></p>
  <p>本文:<%= @blog.content %></p>
  <%= form.submit "登録する" %>
  <%= form.submit "戻る", name: 'back' %>
<% end %>
